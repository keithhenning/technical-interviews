# Creating lists
my_list = [1, "hello", 3.14, True]  # Different types
my_list.append(42)      # Adds to end
my_list.insert(0, "start")  # Adds at position
popped_value = my_list.pop()  # Removes last element
my_list.extend([5, 6, 7])  # Adds multiple elements